# Proof: A K=19 binary tree of eml operations evaluates to x + y

- **Generated:** 2026-04-16
- **Verdict:** PROVED
- **Audit trail:** [proof_audit.md](proof_audit.md) | [proof.py](proof.py)

## Evidence Summary

| ID | Fact | Verified |
|----|------|----------|
| A1 | Token count K = 19 (9 eml operations + 10 leaves) | Computed: 9 eml + 10 leaves = 19 |
| A2 | Step-by-step symbolic evaluation: E9 = x + y | Computed: all 5 critical residuals = 0 |
| A3 | Full expression minus (x + y) = 0 | Computed: residual = 0 |
| A4 | Numerical spot-check at 8 real-valued (x, y) pairs | Computed: max |diff| = 1.26e-14 |
| A5 | Numerical spot-check at 4 complex-valued (x, y) pairs | Computed: max |diff| = 1.34e-15 |

## Proof Logic

The binary operator eml(a, b) = exp(a) - ln(b) is a single operation that, through nesting, can express all elementary functions. The claim is that the specific 19-token expression

```
eml(1, eml(eml(eml(1, eml(eml(1, eml(1, eml(x, 1))), 1)), eml(y, 1)), 1))
```

evaluates to x + y for all real x and y.

The proof first verifies this expression contains exactly 9 eml operations and 10 leaves (from {1, x, y}), confirming K = 19 (A1).

The expression is then unwound layer by layer through 9 intermediate sub-expressions. At each layer, SymPy's symbolic algebra engine evaluates the eml operation and simplifies. Five critical algebraic cancellation points are verified with exact zero residuals (A2):

1. **E1 = exp(x):** The innermost eml(x, 1) evaluates to exp(x) - ln(1) = exp(x).
2. **E2 = e - x:** Wrapping gives eml(1, exp(x)) = e - ln(exp(x)) = e - x.
3. **E4 = exp(e)/(e-x):** The triple-nesting pattern (E3, E4) produces exp(e)/(e-x).
4. **E5 = ln(e-x):** Completing the triple-nesting yields the natural logarithm of (e - x).
5. **E7 = e - x - y:** The subtraction identity eml(ln(a), exp(b)) = a - b gives (e-x) - y.

The final two steps reverse the chain: E8 = exp(e - x - y) and E9 = e - ln(exp(e - x - y)) = e - (e - x - y) = x + y. SymPy confirms that simplify(E9 - (x + y)) = 0 exactly (A3).

As an independent cross-check, the full 9-layer chain was evaluated numerically at 12 test points. Eight real-valued (x, y) pairs spanning the range [-100, 100] all agreed with x + y to within machine epsilon, with the largest discrepancy being 1.26e-14 (A4). Four complex-valued pairs (within the principal-branch domain |Im(x+y)| < pi) likewise agreed, with maximum discrepancy 1.34e-15 (A5).

## What Could Challenge This Verdict?

**Large real x (x > e):** When x exceeds e, the intermediate value E2 = e - x is negative, causing subsequent logarithms to produce complex intermediate values with imaginary parts of +/-pi. These imaginary components cancel exactly across the chain, as confirmed by the numerical test at x = 100, y = -99 (|diff| < 2e-14).

**Complex branch cuts:** On the principal branch of the complex logarithm, log(exp(z)) = z holds only when |Im(z)| <= pi. The identity holds numerically for all complex x, y satisfying |Im(x + y)| < pi. For larger imaginary parts, the error is always a multiple of 2*pi*i. In the paper's formal algebraic framework (where ln and exp are true inverses), the identity holds without restriction.

**Minimality of K = 19:** An exhaustive search of all eml binary trees with leaves {1, x, y} through K = 17 (18.5 million distinct values) found no expression evaluating to x + y, consistent with K = 19 being the minimum code length for addition.

**Singularity at x = e:** The intermediate expression E2 = e - x equals zero when x = e exactly, making E3 = eml(1, 0) undefined. This is a removable singularity: the limit of the full expression as x approaches e equals e + y = x + y. The identity holds for all real x except x = e, where it is undefined but has the correct limit.

## Conclusion

**Verdict: PROVED.** The 19-token expression eml(1, eml(eml(eml(1, eml(eml(1, eml(1, eml(x, 1))), 1)), eml(y, 1)), 1)) evaluates to x + y. This was verified symbolically by SymPy (exact zero residual for real x, y) and numerically at 12 test points (maximum discrepancy 1.26e-14). The expression uses exactly 9 eml operations and 10 leaves, confirming K = 19. An exhaustive search through K = 17 found no shorter expression for addition.

---

Generated by [proof-engine](https://github.com/yaniv-golan/proof-engine) v1.18.0 on 2026-04-16.
